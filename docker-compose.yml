version: '3.8'
services:
backend:
build: .
ports:
- "8000:8000"
volumes:
- ./backend:/app
- ./data:/app/data
- ./models:/app/models
environment:
- MODEL_DIR=/app/models
- DATA_DIR=/app/data
# for GPU: uncomment and configure nvidia runtime on host
# deploy:
# resources:
# reservations:
# devices:
# - driver: nvidia
# capabilities: [gpu]
